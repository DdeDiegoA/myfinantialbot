# LLM Configuration
LLM_PROVIDER=nvidia
LLM_MODEL=deepseek-ai/deepseek-v4-flash
LLM_API_KEY=your-key-here

# Fallback configuration (Nvidia's direct endpoint has a known HTTP 500 bug; use OpenRouter as workaround)
# LLM_PROVIDER=openrouter
# LLM_MODEL=deepseek/deepseek-chat
